package com.unpainperdu.premierpainmod.client.particle.beer_particle.white;

import com.unpainperdu.premierpainmod.client.particle.beer_particle.BeerFoamParticle;
import net.minecraft.client.multiplayer.ClientLevel;
import net.minecraft.client.particle.SpriteSet;

public class WhiteBeerFoamParticle extends BeerFoamParticle
{
    protected WhiteBeerFoamParticle(ClientLevel level, double x, double y, double z, SpriteSet spriteSet)
    {
        super(level, x, y, z, spriteSet);
    }
}
