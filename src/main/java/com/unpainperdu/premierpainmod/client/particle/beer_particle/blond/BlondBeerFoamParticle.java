package com.unpainperdu.premierpainmod.client.particle.beer_particle.blond;

import com.unpainperdu.premierpainmod.client.particle.beer_particle.BeerFoamParticle;
import net.minecraft.client.multiplayer.ClientLevel;
import net.minecraft.client.particle.SpriteSet;

public class BlondBeerFoamParticle extends BeerFoamParticle
{
    protected BlondBeerFoamParticle(ClientLevel level, double x, double y, double z, SpriteSet spriteSet)
    {
        super(level, x, y, z, spriteSet);
    }
}
