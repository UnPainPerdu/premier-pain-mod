package com.unpainperdu.premierpainmod.client.particle.beer_particle.black;

import com.unpainperdu.premierpainmod.client.particle.beer_particle.BeerFoamParticle;
import net.minecraft.client.multiplayer.ClientLevel;
import net.minecraft.client.particle.SpriteSet;

public class BlackBeerFoamParticle extends BeerFoamParticle
{
    protected BlackBeerFoamParticle(ClientLevel level, double x, double y, double z, SpriteSet spriteSet)
    {
        super(level, x, y, z, spriteSet);
    }
}
